server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /chat {
        proxy_pass http://agent-backend:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /health {
        proxy_pass http://agent-backend:8001;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
